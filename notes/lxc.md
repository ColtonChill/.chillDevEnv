lxc config device add <name of container> <name of device>(tak connector) proxy listen=tcp:0.0.0.0:port
lxc config device add <name of container> <name of device>(tak connector) proxy connect=tcp:0.0.0.0:port
