-- This file is for all the options that need to happen last so they 
-- can over ride defaults rather then the defaults overriding them.

-- Change default spelling underline color
vim.cmd("hi SpellBad cterm=undercurl gui=undercurl guisp=#55b5ff")
